# Copy to .env, fill in both values and keep the file private. Git ignores .env.

# Separate admin bearer token, for example: openssl rand -hex 32
CLAN_ADMIN_TOKEN=

# Standard Base64 of 32 random bytes, for example: openssl rand -base64 32
# Keep it: stored credentials cannot be read with a different key.
CLAN_ENCRYPTION_KEY=
